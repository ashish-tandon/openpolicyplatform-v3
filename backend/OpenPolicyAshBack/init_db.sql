-- OpenPolicy Database Initialization
-- This is a minimal SQL file since database initialization is handled by Python
-- The actual schema and data are created by init_database.py

-- Just ensure the database exists and is ready for connections
SELECT 'OpenPolicy Database Ready' as status;