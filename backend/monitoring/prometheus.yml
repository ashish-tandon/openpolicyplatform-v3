global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'openpolicy-api'
    metrics_path: /metrics
    static_configs:
      - targets: ['openpolicy-platform:8000']

rule_files:
  - /etc/prometheus/alert.rules.yml

# Uncomment if you expose nginx ingress metrics
# - job_name: 'nginx-ingress'
#   static_configs:
#     - targets: ['nginx-ingress-controller:10254']
